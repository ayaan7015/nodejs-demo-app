# Initialize npm (if not done)
npm init -y

# Install express
npm install express

# Add Dockerfile (done manually)

# Add all files to git
git add .

# Commit
git commit -m "Prepare Node.js app with Dockerfile and CI/CD workflow"

# Push to GitHub
git push origin main
