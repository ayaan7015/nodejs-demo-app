name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.ayaan7015 }}
          password: ${{ secrets.dckr_pat_F30kDOhqmG25xbIZGgsUhpVaT7g }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.ayaan7015 }}/nodejs-demo-app:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.dckr_pat_F30kDOhqmG25xbIZGgsUhpVaT7g }}/nodejs-demo-app:latest
